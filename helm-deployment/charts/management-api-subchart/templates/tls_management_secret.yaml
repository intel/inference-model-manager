apiVersion: v1
kind: Secret
metadata:
  name: tls-management-secret
  labels:
    app: {{ template "fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: kubernetes.io/tls
data:
  "tls.crt": |-
{{ .Files.Get "certs/man-api-server.crt" | b64enc | indent 4 }}
  "tls.key": |-
{{ .Files.Get "certs/man-api-server.key" | b64enc | indent 4 }}
